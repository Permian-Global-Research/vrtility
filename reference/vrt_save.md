# Save a vrt_block object to disk

Save a vrt_block object to disk

## Usage

``` r
vrt_save(x, outfile)
```

## Arguments

- x:

  A `vrt_stack` of `vrt_block` object.

- outfile:

  A character string of the output file
